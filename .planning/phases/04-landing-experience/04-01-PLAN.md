---
phase: 04-landing-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/landing-examples.ts
  - app/components/LandingHero.tsx
  - app/components/ExampleShowcase.tsx
  - app/components/LandingFooter.tsx
  - app/page.tsx
autonomous: true

must_haves:
  truths:
    - "Landing page displays at root URL (anchor.band)"
    - "Headline and subtext explain what Anchor is"
    - "Get Started button links to /signin"
    - "Three example profiles displayed with era labels"
  artifacts:
    - path: "lib/landing-examples.ts"
      provides: "Mock profile data for 3 examples"
      exports: ["LANDING_EXAMPLES"]
    - path: "app/components/LandingHero.tsx"
      provides: "Hero section with headline, subtext, CTA"
      min_lines: 20
    - path: "app/components/ExampleShowcase.tsx"
      provides: "Example profiles display"
      min_lines: 30
    - path: "app/components/LandingFooter.tsx"
      provides: "Minimal footer with copyright"
      min_lines: 10
    - path: "app/page.tsx"
      provides: "Landing page assembly"
      contains: "LandingHero"
  key_links:
    - from: "app/page.tsx"
      to: "app/components/LandingHero.tsx"
      via: "import and render"
      pattern: "import.*LandingHero"
    - from: "app/components/LandingHero.tsx"
      to: "/signin"
      via: "Link component"
      pattern: "href=\"/signin\""
    - from: "app/components/ExampleShowcase.tsx"
      to: "lib/landing-examples.ts"
      via: "import LANDING_EXAMPLES"
      pattern: "LANDING_EXAMPLES"
---

<objective>
Create the complete landing page for Anchor.band with hero section, example profiles showcase, and footer.

Purpose: New visitors understand what Anchor is and can start the signup flow. Marketing page that converts visitors to users.

Output: Working landing page at root URL with all components, ready for visual polish.
</objective>

<execution_context>
@/Users/mathewbroughton/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mathewbroughton/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-landing-experience/04-CONTEXT.md
@.planning/phases/04-landing-experience/04-RESEARCH.md

# Existing component for reference (profile styling)
@app/components/PublicProfile.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create mock profile data for landing examples</name>
  <files>lib/landing-examples.ts</files>
  <action>
Create TypeScript file with mock profile data for 3 example profiles demonstrating era diversity:

1. **Classic Rock** profile:
   - Handle: "classicfan", displayName: "Alex"
   - Bio: 2-3 sentences, follows anti-cringe rules (no emojis, hype words)
   - 5-6 artists: Led Zeppelin, Pink Floyd, The Beatles, etc.
   - 3-5 albums with captions
   - 3 tracks

2. **Modern Pop** profile:
   - Handle: "tunes", displayName: "Jordan"
   - Bio: 2-3 sentences, anti-cringe compliant
   - Contemporary artists: Frank Ocean, SZA, Tyler the Creator, etc.
   - Albums with captions

3. **Underground/Emerging** profile:
   - Handle: "deepcuts", displayName: "Sam"
   - Bio: 2-3 sentences, anti-cringe compliant
   - Lesser-known artists, diverse genres
   - Albums with captions

Data structure should match existing types (Artist, Album, Track from @/types/music).

Export as `LANDING_EXAMPLES` array with fields:
- id, label (era), handle, displayName, bio
- topArtists, topAlbums (with captions), topTracks
- imagePath (for screenshot: `/examples/{id}-profile.png`)

Use placeholder image URLs for artist/album images (real Spotify image URLs not needed - these are mock examples).
  </action>
  <verify>
- File exists at lib/landing-examples.ts
- TypeScript compiles: `npx tsc --noEmit lib/landing-examples.ts` (no errors)
- Exports LANDING_EXAMPLES with 3 profiles
  </verify>
  <done>
Three distinct example profiles with era diversity, anti-cringe bios/captions, and proper TypeScript types.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create landing page components</name>
  <files>
app/components/LandingHero.tsx
app/components/ExampleShowcase.tsx
app/components/LandingFooter.tsx
  </files>
  <action>
**LandingHero.tsx:**
- Server component (no "use client")
- Headline: "Your music profile, tastefully done" (text-5xl md:text-6xl font-bold)
- Subtext: "Show your taste without the cringe" (text-xl md:text-2xl text-neutral-600)
- CTA button: "Get Started" linking to /signin
- Use Next.js Link component for CTA
- Light background: bg-gradient-to-b from-neutral-50 to-neutral-100 (differentiate from dark profile pages)
- Button styling: bg-neutral-900 text-white px-8 py-4 rounded-lg hover:bg-neutral-800 transition-colors
- Generous vertical padding (py-24 md:py-32)
- Center aligned, max-w-3xl container

**ExampleShowcase.tsx:**
- Server component
- Import LANDING_EXAMPLES from lib/landing-examples
- Section title: "See it in action" (text-2xl font-semibold mb-8)
- Display 3 profile preview images in responsive grid
- Grid: grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8
- Each example card:
  - Image using next/image (public/examples/{id}-profile.png, placeholder blur)
  - Era label above: text-sm font-medium uppercase tracking-wide text-neutral-500
  - Rounded corners, subtle shadow (shadow-sm)
  - Fallback for missing images (neutral placeholder div with label)
- Light background continuation (bg-neutral-50)
- Max-w-5xl container, centered

**LandingFooter.tsx:**
- Server component
- Ultra-minimal: copyright text only
- Text: "Copyright 2026 Anchor.band"
- Styling: text-sm text-neutral-400 py-8 text-center
- No links (per project decisions - Privacy/Terms pages don't exist in v1)
  </action>
  <verify>
- All three component files exist
- No TypeScript errors: `npx tsc --noEmit`
- LandingHero has Link to /signin
- ExampleShowcase imports LANDING_EXAMPLES
  </verify>
  <done>
Three landing page components following design decisions: lighter aesthetic, minimal copy, prominent CTA.
  </done>
</task>

<task type="auto">
  <name>Task 3: Replace root page with landing page</name>
  <files>app/page.tsx</files>
  <action>
Replace the current placeholder page.tsx with the landing page assembly.

Structure:
```tsx
import { LandingHero } from "@/app/components/LandingHero";
import { ExampleShowcase } from "@/app/components/ExampleShowcase";
import { LandingFooter } from "@/app/components/LandingFooter";
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Anchor.band - Your music profile, tastefully done",
  description: "Create a shareable music profile page. Connect Spotify, generate tasteful content, and share your taste.",
  openGraph: {
    title: "Anchor.band",
    description: "Your music profile, tastefully done",
    images: ["/og-image.png"],
    siteName: "Anchor.band",
  },
  twitter: {
    card: "summary_large_image",
    title: "Anchor.band",
    description: "Your music profile, tastefully done",
    images: ["/og-image.png"],
  },
};

export default function LandingPage() {
  return (
    <>
      <LandingHero />
      <ExampleShowcase />
      <LandingFooter />
    </>
  );
}
```

Key points:
- Remove old placeholder content entirely
- Add SEO metadata for landing page
- Server component (no "use client")
- Simple composition of three sections
- No authentication checks (landing page is public)
  </action>
  <verify>
- app/page.tsx renders landing components
- `npm run dev` starts without errors
- Visit http://localhost:3000 shows landing page (not old placeholder)
- Page source contains metadata title "Anchor.band - Your music profile"
  </verify>
  <done>
Root URL displays complete landing page with hero, examples, and footer. SEO metadata present for social sharing.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `npm run build` succeeds (no TypeScript/build errors)
2. `npm run dev` and visit http://localhost:3000
3. Landing page displays with:
   - Headline "Your music profile, tastefully done"
   - Subtext "Show your taste without the cringe"
   - "Get Started" button
   - "See it in action" section with 3 example cards
   - Footer with copyright
4. Click "Get Started" navigates to /signin
5. Page uses light theme (distinct from dark profile pages)
</verification>

<success_criteria>
- Landing page renders at root URL
- All three requirements addressed:
  - LAND-01: Headline and subtext explain what Anchor is
  - LAND-02: Three example profile placeholders displayed
  - LAND-03: "Get Started" button links to /signin
- TypeScript compiles without errors
- Responsive layout (mobile + desktop)
- Lighter aesthetic differentiates landing from product pages
</success_criteria>

<output>
After completion, create `.planning/phases/04-landing-experience/04-01-SUMMARY.md`
</output>
